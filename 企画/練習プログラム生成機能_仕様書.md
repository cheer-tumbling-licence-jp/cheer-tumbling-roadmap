# 練習プログラム生成機能 仕様書

> チーム人数・指導者数・目標から、サーキット練習プログラムを自動生成する機能。
> コーチプラン（有料）の目玉機能のひとつ。
> 関連：`企画/マーケティング企画書.md`、`企画/進捗トラッキング機能_仕様書.md`

- 作成：2026-05-17（CEO=Claude）
- ステータス：**v1（MVP）実装完了／監督レビュー待ち**

---

## 0. 進捗・次アクション

### 完了
- 機能要件・生成アルゴリズム・UI・データモデル・開発フェーズの定義
- **v1（MVP）実装完了**（2026-05-17）
- **v1.1 改善実装完了**（2026-05-17・監督フィードバック反映）：
  - グループ柔軟化：「1グループの人数」入力を追加（2人〜・ステーション数は固定しない）
  - 危険難易度（小/中/大）：全技に `dangerLevels` を設定（叩き台）。技モーダルに危険度バッジ、
    プログラムの各技に危険度チップを表示
  - プログラムの目的＋各ステーションの目的を表示。安全注意文に危険度の確認を追記
- **v1.2 監督パターン対応**（2026-05-17）：
  - `programPatterns` を新設。監督が現場の練習の型（ステーション構成）を登録すると、
    その目標を選んだとき「プログラムを作る」がパターン通りに生成する
  - 目標セレクターにパターン有りの目印（📋）、結果にパターン名バッジを表示
  - 登録済みパターン：「バク転習得サーキット」（監督提供・④⑤のねらいはCEO下書き）
- **v1.3 カテゴリ方式の自動生成**（2026-05-18・監督提案）：
  - パターンが無い目標は、標準カテゴリ構成でアプリが自動でステーションを組む：
    ①補助して実施 ②細分化練習 ③トレーニング① ④トレーニング② ⑤仕上げ（self）
  - 各カテゴリの中身は目標技のデータから自動選択（補助/仕上げ＝目標技、
    細分化＝前段階技、トレーニング＝必要トレーニング）
  - 指導者2人なら⑤仕上げにも指導者を配置
  - 監督は全パターンを手書きせず、カテゴリ構成に乗せるだけでよい
  - 「1グループの人数」入力は廃止（カテゴリ構成がステーション数を決め、人数はグループ人数で吸収）
- **v1.4 テーマ目標**（2026-05-18・監督要望）：
  - 技だけでなく「テーマ」を目標に選べる。目標セレクターに「テーマ（目的別）」を追加
  - `programThemes` を新設。登録テーマ3つ（叩き台・監督添削予定）：
    基礎づくり／柔軟性アップ／基礎運動
  - テーマ選択時は、そのテーマ専用のサーキットを生成（結果に「🎯 テーマ：◯◯」バッジ）
  - テーマの中身（ステーション・ねらい）はCEO叩き台。監督が添削して確定する

### 次にやること
1. **監督レビュー**：v1.1 の動作を確認
2. **危険難易度の添削**：`dangerLevels` の値（小/中/大）を監督が確定
3. **テーマ目標**の追加（基礎づくり等）← 監督要望。専用ステップで設計
4. プログラム保存機能の追加（§3.5）

### 監督の判断が必要
- [ ] 危険難易度（小/中/大）の各技の値（現在は叩き台）
- [ ] ステーションの目的文・安全注意文の文言
- [ ] テーマ目標として用意するテーマ（基礎づくり／柔軟性／ジャンプ強化 等）

---

## 1. 目的とゴール

初心者指導者・チームコーチが、**限られた指導者数で安全かつ効率的にタンブリング練習を回せる**よう、チーム人数と目標からサーキット練習プログラムを自動生成する。

**現場の課題**：タンブリングは補助が必要 → 指導者は物理的に1ヶ所にしか入れない → 補助に入っている間、残りの選手をどう安全に自走させるかが現場最大の悩み。本機能はこれを自動で解決する。

**狙い**：監督が現場で実践しているサーキット練習の組み立て方を、ソフトが再現する。初心者指導者でもプロの練習設計ができるようになる。

---

## 2. ユーザーストーリー

- **初心者指導者**：「15人を1人で見る。バク転を教えたいが、補助に入ると他の10人が手持ち無沙汰になる。どう組めばいいか分からない」
- **チームコーチ**：「人数も指導者数も日によって変わる。毎回ゼロから練習メニューを組むのが大変」
- **監督（出張レッスン）**：「訪問先のチームに『この人数ならこう回して』を渡せると、レッスン後のフォローになる」

---

## 3. 機能要件

### 3.1 入力
- **チーム人数**（必須）
- **指導者人数**（必須・既定1）
- **目標**（必須）：習得を目指す技を選ぶ（例：バク転）。※テーマ選択は §9 で検討
- **練習時間**（任意・既定30分）：ローテーション時間の自動計算に使用

### 3.2 生成ロジック（概要）
- 1グループ標準人数（既定5人）から **ステーション数** を算出
- **指導者の数だけ「補助ステーション」**を作る（＝1人1ヶ所しか入れない現実を反映）
- 残りは「**自走ステーション**」＝補助不要の体づくり・ドリル
- アルゴリズム詳細は §5

### 3.3 出力（プログラム表示）
- **ステーション一覧**（カード形式）：番号／役割（🧑‍🏫指導者つき or 自走）／タイトル／内容（技・トレーニング）／1グループの人数
- **ローテーション説明**：「1ステーション◯分 × ◯周で全員が全ステーションを通過」
- **印刷対応**（現場で紙に出して使えるよう）

### 3.4 補助ステーションの安全ロジック（本機能の核心）
- **補助が必要な技（＝目標技そのもの）は必ず「指導者ステーション」に配置**する
- **自走ステーションには補助不要のものだけ**を置く：
  - トレーニング（筋力・体幹・柔軟）＝補助不要
  - 前段階の技のうち「自分だけで安全にできるドリル」（例：ブリッジ、後方ウォークオーバー）
- ⚠️ **危険な技を自走ステーションに絶対に置かない**。そのため「自走に置いてよい技」の基準が必須（§9 未決事項）。

### 3.5 保存・連携
- 生成したプログラムを保存（複数保存可）— v1
- 各ステーションを「練習メニュー」に書き出せる — v2
- 印刷 — v1

### 3.6 スコープ外（v1では作らない）
- テーマ目標（基礎づくり等）の自動構成（v2）
- 指導者3人以上の複雑な配分最適化（v2）
- 成長記録（選手の習得状況）と連動した内容自動調整（v3）

---

## 4. 画面・UI設計

### 4.1 配置
**新しい5つ目のタブ「練習プログラム」**（CEO推奨）。コーチプランの目玉機能であり、発見されやすい場所に置く。タブは折り返し表示（flex-wrap）に対応済み。
※代替案：練習メニュータブ内の一機能とする（§9）。

### 4.2 生成画面
- 上部に**入力フォーム**：人数・指導者数・目標（技セレクト）・練習時間
- 「**プログラムを作る**」ボタン
- 下部に**生成結果**：ステーションカード＋ローテーション説明

### 4.3 生成結果のイメージ（15人・指導者1人・バク転）

```
▶ サーキット練習プログラム
  15人 / 指導者1人 / 目標：バク転 / 30分

  3ステーション・1グループ5人・1ステーション10分でローテーション

  ┌────────────────────────────┐
  │ ① 🧑‍🏫 補助つきバク転        5人 │
  │    バク転（マット・補助あり）      │
  ├────────────────────────────┤
  │ ② 自走 体づくり             5人 │
  │    ホロウホールド／アーチホールド   │
  │    ／スクワットジャンプ          │
  ├────────────────────────────┤
  │ ③ 自走 前段階ドリル          5人 │
  │    ブリッジ／後方ウォークオーバー   │
  └────────────────────────────┘
```

---

## 5. 生成アルゴリズム（詳細）

```
generateProgram({ teamSize, instructorCount, targetSkillId, totalMinutes }):
  GROUP_SIZE = 5                                  // 1グループ標準人数（§9で確定）
  stationCount = clamp(round(teamSize / GROUP_SIZE), 2, 6)
  groupSize    = ceil(teamSize / stationCount)
  skill        = skillById[targetSkillId]

  // --- 補助ステーション（指導者数だけ）---
  spotCount = min(instructorCount, stationCount)
  stations  = []
  for i in 1..spotCount:
     stations.push({ role:'spot', title:'補助つき'+skill.name, items:[skill技] })

  // --- 自走ステーションのコンテンツプール ---
  conditioning = skill.trainings                  // 体づくり（補助不要）
  drills       = skill.prereqs.filter(自走可能な技) // 前段階ドリル（§9の基準で判定）

  selfCount = stationCount - spotCount
  // 体づくり1枠＋ドリル1枠を基本に、selfCount に応じて分割・配分
  stations += buildSelfStations(selfCount, conditioning, drills)

  rotationMinutes = floor(totalMinutes / stationCount)
  return { stations, stationCount, groupSize, rotationMinutes }
```

**エッジケース**
- `teamSize ≤ GROUP_SIZE`：サーキット不要 → 「1グループで通し練習」を提案
- `instructorCount ≥ stationCount`：全ステーションが補助つき（自走ゼロ）
- 自走コンテンツが足りない：ステーション数を減らす、または内容を合流

---

## 6. データモデル

新規 localStorage キー **`cheerTumblingPrograms_v1`**（保存機能用・v1後半）：

```json
{
  "version": 1,
  "programs": [
    {
      "id": "prog_xxx",
      "name": "15人バク転サーキット",
      "teamSize": 15,
      "instructorCount": 1,
      "targetSkillId": "standing-bhs",
      "totalMinutes": 30,
      "createdAt": "2026-05-17T..."
    }
  ]
}
```

- プログラムは入力値を保存し、表示時に毎回 `generateProgram()` で再生成（生成結果は保存しない＝技データ更新に自動追従）。
- 生成ロジックは既存の `skills[].trainings` / `skills[].prereqs` を参照（新データ不要）。

---

## 7. 無料／有料ゲーティング

- **無料**：サンプルのサーキットプログラムを1つだけ固定表示（例：15人・指導者1人・バク転）。機能の価値を見せて販促。
- **コーチプラン（有料）**：人数・指導者数・目標を入力して**自動生成し放題**＋保存・印刷。
- MVP段階のゲート方式はトライアルコード方式（マーケティング企画書 §11 と同方針）。

---

## 8. 開発フェーズ

| 版 | 内容 |
|---|---|
| **v1（MVP）** | 入力フォーム／生成アルゴリズム（技ベース目標）／結果表示／印刷／サンプル1つ |
| **v2** | テーマ目標（基礎づくり等）、プログラム保存・呼び出し、ステーション→練習メニュー書き出し、指導者複数の配分改善 |
| **v3** | 成長記録（選手の習得状況）と連動し、チームの到達度に応じて内容を自動調整 |

---

## 9. 未決事項（監督の判断が必要）

1. **1グループの標準人数**：既定5人でよいか。4〜6で可変にするか。
2. **自走ステーションに置いてよい技の安全基準**（最重要・安全に直結）：
   - 案A：レベル閾値（例：レベル1以下の技は自走可）
   - 案B：技データに「自走可フラグ」を新設し、監督が技ごとに指定
   - CEO推奨：案B（監督が技ごとに安全判断できる。安全機能なので明示指定が確実）
3. **配置**：新タブ「練習プログラム」（推奨）か、練習メニュータブ内の一機能か。
4. **目標の選び方**：v1は「技を選ぶ」のみでよいか。「テーマ（基礎づくり・ジャンプ強化 等）」も選べるようにするか（v2候補）。
5. **指導者2人以上**：補助ステーションを単純に増やす方式でよいか。サーキットを2系統に分ける案も将来検討。
6. **ローテーション時間**：練習時間入力からの自動計算でよいか（休憩・説明時間の控除は入れるか）。

---

## 10. CEO判断ログ

| 日付 | 決定 | 根拠 |
|---|---|---|
| 2026-05-17 | 生成方式はルールベースのジェネレーター（AI不使用） | 既存の skill→trainings/prereqs データで実装可能。動作が予測でき軽量 |
| 2026-05-17 | 補助が必要な目標技は必ず指導者ステーションに固定配置 | 安全の核。自走ステーションには補助不要のものだけを置く |
| 2026-05-17 | 生成結果は保存せず入力値のみ保存し都度再生成 | 技データ更新に自動追従。データ整合性を保つ |
| 2026-05-17 | コーチプラン（有料）の目玉機能と位置づけ | 初心者指導者の最大の悩みを解決。競合が真似しにくい監督の知見そのもの |
