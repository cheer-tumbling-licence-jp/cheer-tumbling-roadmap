.DS_Store
.claude/
*.swp
*~
node_modules/

# Cowork 同期検出時の退避ファイル（マージ後は不要）
.cowork_pending/

# YouTube アップロード用のローカル動画コピー（容量が大きいので追跡しない）
youtube/videos/
youtube/uploaded.json
youtube/uploaded.json.bak_*
youtube/token.json
youtube/launchd.log
youtube/launchd.err

# 編集前バックアップ（ローカル保存のみ、Git には含めない）
backup-*/

# Firestore 日次バックアップ（ユーザーのemail等個人情報含むため Git から除外）
backups/
